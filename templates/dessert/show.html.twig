{% extends 'base.html.twig' %}

{% block title %}Dessert{% endblock %}

{% block body %}
    <h1>Dessert</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dessert.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ dessert.image }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ dessert.title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ dessert.price }}</td>
            </tr>
            <tr>
                <th>Ingredients</th>
                <td>{{ dessert.ingredients }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dessert_index') }}">back to list</a>

    <a href="{{ path('app_dessert_edit', {'id': dessert.id}) }}">edit</a>

    {{ include('dessert/_delete_form.html.twig') }}
{% endblock %}
