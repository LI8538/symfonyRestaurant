{% extends 'base.html.twig' %}

{% block title %}Starter{% endblock %}

{% block body %}
    <h1>Starter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ starter.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ starter.image }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ starter.title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ starter.price }}</td>
            </tr>
            <tr>
                <th>Ingredients</th>
                <td>{{ starter.ingredients }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_starter_index') }}">back to list</a>

    <a href="{{ path('app_starter_edit', {'id': starter.id}) }}">edit</a>

    {{ include('starter/_delete_form.html.twig') }}
{% endblock %}
